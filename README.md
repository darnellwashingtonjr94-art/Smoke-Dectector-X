# Smoke-Dectector-X
an edge-AI smart home safety device. It pairs a standard smoke sensor with a 360-degree camera and onboard machine learning. When smoke triggers the system, it wakes the camera to scan for visible flames using a custom YOLOv8 model, sounding the alarm and dispatching network alerts only upon positive visual confirmation.
